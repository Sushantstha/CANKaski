<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tribikram.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Activities.SecondActivity" />
        <activity android:name=".Activities.ThirdActivity" />
        <activity android:name=".Activities.FourthActivity" />
        <activity android:name=".Activities.FifthActivity" />
        <activity android:name=".Activities.SixthActivity" >

        </activity>
        <activity android:name=".Activities.SeventhActivity" />
        <activity android:name=".Activities.EightActivity" />
        <activity android:name=".Activities.NinthActivity" />
        <activity android:name=".Activities.TenthActivity" />
        <activity android:name=".Activities.EleventhActivity" />
        <activity android:name=".Activities.OptionMenuActivity" />
        <activity android:name=".Activities.DialogActivity" />
        <activity android:name=".Activities.PreferenceActivity" />
        <activity android:name=".Activities.RoomActivity" />
        <activity android:name=".DatabaseRecyclerAcitivty" />
        <activity android:name=".Activities.FileStorageActivity" />
        <activity android:name=".AsyncActivity" />
        <activity android:name=".NetworkActivity" />
        <activity android:name=".FlowersDetailsActivity" />
        <activity android:name=".Activities.RetrofitActivity" />
        <activity android:name=".CreateFlowerActivity" />
        <activity
            android:name=".Activities.BasicActivity"
            android:label="@string/title_activity_basic"
            android:theme="@style/AppTheme.NoActionBar" >

        </activity>
        <activity
            android:name=".Activities.CollapsingToolbarActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.TabbedActivity"
            android:label="@string/title_activity_tabbed"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.NavigationDrawerActivity"
            android:label="@string/title_activity_navigation_drawer"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.BottomNavActivity"
            android:label="@string/title_activity_bottom_nav"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name=".Activities.MapActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>

        <service
            android:name=".MyFirebaseInstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

        <service
            android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

    </application>

</manifest>